export enum tokenActionTypes {
    GET_SESSION_TOKEN = '[Token] get session token',
    GET_SESSION_TOKEN_SUCCESS = '[Token] get session token success',
    GET_SESSION_TOKEN_ERROR = '[Token] get session token error',
}
