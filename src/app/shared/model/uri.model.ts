/* istanbul ignore next */
export class Uri {
    public title: string;
    public description: string;
    public uri: string;

    constructor(title: string, description: string, uri: string) {
        this.title = title;
        this.description = description;
        this.uri = uri;
    }
}
