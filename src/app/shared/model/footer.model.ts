import { TopUri } from './top-uri.model';
/* istanbul ignore next */
export class Footer {
    public footer: Array<TopUri>;

    constructor(footer: Array<TopUri>) {
        this.footer = this.footer;
    }
}
