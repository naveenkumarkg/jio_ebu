import { TopUri } from './top-uri.model';
/* istanbul ignore next */
export class Header {
    public header: Array<TopUri>;

    constructor(header: Array<TopUri>) {
        this.header = this.header;
    }
}
